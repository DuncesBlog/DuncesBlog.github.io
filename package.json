{
  "devDependencies": {
    "vitepress": "^1.2.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev Notes",
    "docs:build": "vitepress build Notes",
    "docs:preview": "vitepress preview Notes"
  }
  // scripts 运行示例：
  // 在命令行中运行
  // npm run docs:dev
  // 或
  // npx vitepress dev Notes
}